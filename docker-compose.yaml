services:
  mongo:
    image: mongo:6.0
    container: momgodb
    restart: always
    ports:
      - "27017:27017"
    enviroments:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin123
    volumes:
      -mongo_data:/data/db

volumes:
  mongo_data: